//2. Faça um programa que retorne o maior de dois números. Defina no começo do programa duas constantes com os valores que serão comparados.

const a2 = 13;
const b2 = 7;

if (a2 > b2) {
    console.log("O " + a2 + " é maior que " + b2);
} else {
    console.log("O " + b2 + " é maior que " + a2);
};
